# neura-pos

Neura POS (Laravel-based POS system).

## Setup
- Copy `.env.example` to `.env`
- Configure database and app settings
- Run `composer install`
- Run `php artisan key:generate`
- Run `php artisan migrate --seed`
