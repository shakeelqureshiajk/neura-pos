<input type="{{ $type }}" id="{{$id}}" name="{{ $name }}" value="{{ $value }}" {{ $required ? 'required' : '' }} class="form-control {{ $additionalClasses }}" autocomplete='{{ $autocomplete ? "on" : "off" }}' placeholder="{{ $placeholder }}" {{ $disabled ? 'disabled' : '' }} {{ $autofocus }} {{ $readonly ? 'readonly' : '' }} minlength="{{ $minlength }}" minlength="{{ $minlength }}" maxlength="{{ $maxlength }}" />
